PORT=3001
JWT_SECRET=change-me-to-a-long-random-string
# Generate with: node -e "const c=require('crypto');const h=c.createHash('sha256').update('yourpassword').digest('base64');console.log('PASSWORD_HASH='+h)"
PASSWORD_HASH=
